language: objective-c
before_install:
    - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
    - pod install
script:
    - xcodebuild test -workspace FastEasyMapping.xcworkspace -scheme FastEasyMapping -sdk iphonesimulator
